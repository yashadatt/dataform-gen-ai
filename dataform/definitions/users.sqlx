config { type: "table" }

select
  cast(userId as string)        as userId,
  cast(active as boolean)      as active,
  cast(createdAt1 as timestamp) as createdAt,
  cast(birthDate1 as date)      as birthDate,
  cast(isDeleted1 as boolean) as deletedFlag

from source_users


